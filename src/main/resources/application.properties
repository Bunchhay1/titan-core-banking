# ==========================================
# ? TITAN CORE BANKING CONFIGURATION (HYBRID)
# ==========================================

# ------------------------------------------
# 1. DATABASE CONFIGURATION (PostgreSQL)
# ------------------------------------------
# ? Logic: ??????????? Environment Variable ????? "SPRING_DATASOURCE_URL" (?? Docker) ???????????
# ?????????? (??? Run ????? IntelliJ) ????????? "jdbc:postgresql://localhost:5432/titandb"
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/titandb}

spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:password123}
spring.datasource.driver-class-name=org.postgresql.Driver

# ------------------------------------------
# 2. JPA / HIBERNATE
# ------------------------------------------
spring.jpa.hibernate.ddl-auto=update

# SQL Logging (Keep enabled for debugging)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ------------------------------------------
# 3. REDIS CONFIGURATION
# ------------------------------------------
# ? Logic: ???? "redis-cache" ?????????? Docker, ??? "localhost" ?????????
spring.data.redis.host=${SPRING_DATA_REDIS_HOST:localhost}
spring.data.redis.port=${SPRING_DATA_REDIS_PORT:6379}
# spring.data.redis.password=${SPRING_DATA_REDIS_PASSWORD:}

# ------------------------------------------
# 4. PERFORMANCE (Java 21 Virtual Threads)
# ------------------------------------------
spring.threads.virtual.enabled=true

# ------------------------------------------
# 5. SYSTEM TWEAKS
# ------------------------------------------
spring.jmx.enabled=false

# ------------------------------------------
# 6. SECURITY / JWT CONFIGURATION
# ------------------------------------------
# ?? FIXED: ???????????????? Java, ?????? Config ??? ??????????????????? Property ??????????:
# 14 days in milliseconds
app.jwt.expiration-ms=1209600000
app.jwt.secret=${JWT_SECRET:MySuperSecretKeyForTitanCoreBanking2026}